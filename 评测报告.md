# Zotero Duplicate Cleaner 插件评测报告

- **评测对象**：`C:\Users\Administrator\Desktop\projects\zotero-dedup-plugin`（源码 manifest 版本 0.15.2，Zotero 中实际安装 0.15.1）
- **评测方式**：全量源码走读（bootstrap.js 627 行 + resource-locations.js 121 行 + resource-cleanup.js 159 行 + 构建/CI/测试脚本）+ 34 项 Node 回归测试实跑 + 真实 Zotero 10.0.1 库（1667 活动条目）算法复算
- **评测时间**：2026-09-04

## 总评

**综合 8.2 / 10。** 这是一个安全设计扎实、算法思路正确的实用工具，核心价值在「字节级 SHA-256 判定 + 事务内二次校验 + 只进回收站」这条安全链路，明显优于 Zotero 原生去重的启发式方案。主要短板在**执行效率**（全量重复哈希、循环内全库重取）和**版本/文档漂移**（四处版本号互相矛盾）。

| 维度 | 得分 | 一句话结论 |
|---|---|---|
| 功能完整性 | 9.0 | 四种扫描模式覆盖条目合并、孤儿 PDF、笔记、无效项，功能面完整 |
| 实用性 | 9.0 | 解决真实痛点（本次 TKG 清理中实测验证过同类需求），保护规则体贴 |
| 稳定性 | 8.5 | 34 项回归全过，生命周期处理规范，快照失效检测可靠 |
| 算法设计 | 8.0 | SHA-256 + DOI 归一化 + 类型隔离，判定准确；标题匹配偏保守 |
| 执行效率 | 6.5 | 全库 PDF 每次扫描全量哈希无缓存；两处循环内 O(N×M) 全库重取 |
| 美观易用性 | 8.0 | 卡片 + 资源流图 + 分层折叠设计用心；硬编码浅色主题是硬伤 |
| 工程质量 | 8.5 | 三套回归 + 真实库 fixture 测试 + CI 发布，测试文化少见地好 |
| 文档一致性 | 6.0 | README 标题 0.10.0、构建章节 0.7.0、manifest 0.15.2、已装 0.15.1 |

## 各维度详评

### 1. 功能完整性（9/10）

四种模式入口集中在工具菜单「文献资源整理」面板：

1. **扫描重复**：普通条目（类型 + DOI/完整标题分组）＋ 独立 PDF 与条目内 PDF 的 SHA-256 内容对照 ＋ 完整 HTML 相同的重复笔记，一次扫描出三类候选；
2. **扫描无效项**：无附件条目、全部本地附件缺失的条目、可安全清理的缺失附件记录，远程 URL / 待下载 / 状态不明的不误判；
3. **孤儿 PDF 识别建父条目**：调 Zotero 内置元数据识别，按 10 个一批执行；
4. **统计概览**：条目/附件/PDF/笔记/孤儿 PDF/无效条目一行汇总。

加分项：搜索框实时筛选且全选仅作用于可见结果、资源流图可视化合并前后去向、分类合并预览。减分项：不支持模糊标题匹配（不同标题且无 DOI 的重复发现不了），无「按当前分类扫描」粒度。

### 2. 实用性（9/10）

在本次 TKG 文献库清理实战中，插件针对的「条目 + 孤儿 PDF」重复正是真实高频痛点（该库历史审计出 63 组 65 个字节级相同孤儿 PDF）。保护规则非常体贴：带笔记/批注/关联的附件跳过、唯一条目不因缺附件被清、无附件文献不等于重复、每次默认不勾选、处理前二次确认。**全部删除只进回收站，可恢复**。

### 3. 稳定性（8.5/10）

- **实测**：location-regression 8 项、orphan-regression 14 项、resource-regression 12 项，**34 项全部 PASS**（本次评测实跑）。
- **防失效机制是亮点**：处理时在事务内重新校验条目存在性、父子关系、内容哈希、快照一致性，扫描后状态变化一律抛「请重新扫描」而不是带病执行——这正是同类工具最常见的翻车点。
- **API 用法验证**：`Zotero.Items.trash` 源码要求 `requireTransaction()`，插件确实全部在 `executeTransaction` 内调用，用法正确。
- **生命周期**：菜单注入带 12 次×500ms 重试，`shutdown` 完整清理面板与菜单，`startup` 等待 `initializationPromise`。
- 隐患：orphanMetadata 批处理用 `setTimeout 800ms` 等识别完成，无完成回调校验，大批量时可能漏等待。

### 4. 算法设计（8/10）

- **SHA-256 字节级判定**是本插件核心优势：Zotero 原生去重只比标题/作者/年份/ISBN，无法发现「同 PDF 不同元数据」和「孤儿 PDF」；本插件实测能补上这块（历史审计 63 组字节级相同孤儿 PDF，原生方案全部漏检）。
- DOI 归一化做得细：剥离 `dx.doi.org` / `doi:` 前缀、大小写归一；`itemTypeID` 进 key 避免跨条目类型误并。
- 元信息保留策略合理：先保「有可用 PDF」，再比 12 字段 + 作者的丰富度；合并时 donor 只补空白字段、不覆盖已有值。
- 笔记按完整 HTML 比较，不凭相似文本删，保守正确。
- 保守性边界：标题只做小写 + 空白折叠，标点/副标题差异不会归组（README 已声明，属于有意取舍）。

### 5. 执行效率（6.5/10）—— 最大短板

1. **每次扫描对全库 PDF 全量哈希**：`scan()` 对库内所有 PDF（本库 447 个）逐个 `IOUtils.read` 整文件进内存 + `crypto.subtle.digest`，顺序执行、**无哈希缓存**；点「处理选中」结束后还会自动重扫一次，等于全量再哈希一遍。
2. **两处 O(N×M) 全库重取**：`trashMissingGroupPDFs` 在 per-attachment 循环内、`mergeOrphanPDFs` 在 per-reference 循环内各自调用 `Zotero.Items.getAll(libraryID, false, false)` 重新物化全库。小库无感，万级条目库会明显变慢。
3. `attachmentFingerprint` 整文件读入内存，大 PDF 内存峰值高，未流式处理。
4. 资源流图在 `addGroup` 时即时全量渲染（`flow.hidden=false`），候选组很多时首屏渲染开销偏大。

建议：哈希结果按 itemID+fileSize+mtime 缓存（会话级即可）；循环外取一次 `getAll`；处理完成后只增量重扫受影响类型。

### 6. 美观易用性（8/10）

- 面板 1100×800 自适应（max 94vw/92vh），信息架构清晰：工具栏 → 搜索 → 计数栏 → 状态 → 统计 → 结果滚动区；资源卡片带角色徽章（保留/清理双色）、分类路径 chip、父条目折叠区、判定依据默认折叠，层次分明。
- **资源流图**（合并前 → 处理过程 → 合并后三列流程）是同类工具里少见的可视化设计，保留/移除用绿/红左边框区分，一眼能看懂去向。
- 键盘可访问性完整：Tab 焦点圈、Escape（先清搜索再关面板的两段式）、`role="dialog"`/`aria-modal`/`role="status"` 都有。
- 硬伤：**颜色全部硬编码浅色主题**（白底、#222 文字），Zotero 7 深色主题下会是一块刺眼的白色面板；样式全为内联字符串（单条 style 最长 800+ 字符），调样式要改 JS，且两段 `<style>` 注入的 class（如 `.zotero-dedup-panel-inner{}` 空规则）有明显手工痕迹。

### 7. 工程质量（8.5/10）

- **测试金字塔完整**：Node 纯逻辑回归（mock Zotero API）→ 真实库隔离 fixture 测试（建样本→断言→清理，结果 JSON 留档）→ 真实 UI 交互断言（搜索 10 项），README 里每个版本都附验证记录，这种测试文化在 Zotero 插件生态里属于前 5% 水平。
- CI/CD：tag 触发 windows-latest 构建 XPI 并发 GitHub Release，读版本号自动化，链路完整。
- 打包方案：build.ps1 把三个 JS 合并为单一 bootstrap.js 入口，摆脱旧 chrome.manifest 机制（旧版实测的 `NS_ERROR_FILE_NOT_FOUND`、Services 导入失败等故障均已在架构上移除）。
- 小瑕疵：根目录堆了 8 个 live-fixture / regression 脚本和结果 JSON，未归入 tests/ 目录；git 有未提交的 manifest/bootstrap 修改。

### 8. 发现的具体问题清单

| 级别 | 问题 | 位置 |
|---|---|---|
| P1 | 循环内全库 `getAll` 重取，O(N×M) | bootstrap.js `trashMissingGroupPDFs`（每附件）、`mergeOrphanPDFs`（每引用） |
| P1 | 全量 SHA-256 无缓存 + 处理后自动全量重扫 | bootstrap.js `scan()`、`attachmentFingerprint` |
| P1 | 版本四处漂移：README 标题 0.10.0 / 构建章节 0.7.0 / manifest 0.15.2（未提交）/ 已装 0.15.1 | README.md、manifest.json |
| P2 | 「孤儿 PDF 建父条目」模式下仍渲染「合并后保留位置」区，显示「保留条目无法确认 / 未知分类」，对该模式是误导（该模式是新建父条目而非保留旧条目） | bootstrap.js `addGroup` + resource-locations.js `destination()` |
| P2 | 深色主题不兼容（硬编码浅色） | bootstrap.js 内联样式 |
| P2 | orphanMetadata 批处理靠 800ms 固定等待，无完成校验 | bootstrap.js 「处理选中」handler |
| P3 | 整文件读入内存哈希，大 PDF 内存峰值高 | `attachmentFingerprint` |
| P3 | 测试脚本与结果 JSON 散落根目录；空 CSS 规则 `.zotero-dedup-panel-inner{}` | 根目录、bootstrap.js |

## 真实库实测快照（评测当天）

用插件同款算法对当前文献库复算：1667 个活动条目，**常规条目重复组 0**（当天下午刚完成 13 组 TKG 附件去重，算法结论与人工清理结果一致，交叉验证了判定准确性）；孤儿 PDF 76 个（含尚未对照的潜在 SHA-256 重复）；附件 586 / PDF 447 / 笔记 443。插件历史审计（orphan-audit.json）曾发现 63 组 65 个字节级相同孤儿 PDF，其中 TKG 子树 60 组。

## 改进优先级建议

1. **先补版本一致性**：README 版本号与构建章节更新到 0.15.x，提交 manifest 变更（成本最低、影响观感最大）。
2. **哈希缓存**：`Map<itemID + fileSize + mtime, hash>` 会话级缓存，扫描和处理后重扫速度可提升一个量级。
3. **消除循环内 getAll**：`trashMissingGroupPDFs` / `mergeOrphanPDFs` 循环外取一次，循环内按 id 查 Map。
4. **修 orphanMetadata 的误导性保留位置区**：该模式跳过 destination 渲染，改为提示「将创建新父条目」。
5. **深色主题**：改用 Zotero CSS 变量（`--material-background` 等）或 `prefers-color-scheme`。
6. 工程整理：测试脚本归入 tests/，fixture 结果归入 test-results/。
